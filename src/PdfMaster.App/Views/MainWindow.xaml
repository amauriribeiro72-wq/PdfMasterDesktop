<Window x:Class="PdfMaster.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:PdfMaster.App.Views"
        xmlns:vm="clr-namespace:PdfMaster.App.ViewModels"
        Title="PDF Master Pro - Edição Desktop (Offline &amp; Seguro)"
        Height="740" Width="1120"
        MinHeight="600" MinWidth="880"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource AppBackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Barra de Cabeçalho Superior -->
        <Border Grid.Row="0" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="20,12">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📑" FontSize="20" Margin="0,0,10,0"/>
                    <TextBlock Text="PDF Master Pro" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
                    <Border Background="#E8F4FD" CornerRadius="4" Margin="12,0,0,0" Padding="8,2" VerticalAlignment="Center">
                        <TextBlock Text="Desktop Edition" FontSize="11" FontWeight="SemiBold" Foreground="#0066CC"/>
                    </Border>
                </StackPanel>
                <TextBlock Text="100% Offline &amp; Seguro • Windows 10/11" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
            </Grid>
        </Border>

        <!-- Conteúdo Principal -->
        <views:BatchProcessingView Grid.Row="1" DataContext="{Binding BatchProcessing}" />
    </Grid>
</Window>
